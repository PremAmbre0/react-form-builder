[build]
  command = "npm run build"
  functions = "netlify/functions"
  # Assuming your build output is in 'build' (standard for Create React App) 
  # If you use 'dist', keep it as 'dist'
  publish = "build" 

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

